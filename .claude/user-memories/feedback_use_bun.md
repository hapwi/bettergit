---
name: Use bun not npm
description: User prefers bun as package manager, not npm
type: feedback
---

Always use `bun` for package management (install, add, etc.), never `npm`.

**Why:** User explicitly corrected when npm was used.
**How to apply:** Use `bun add`, `bun add -d`, `bun install`, `bun run` etc. for all package operations in this project and likely others.
